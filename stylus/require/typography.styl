/* VARS */
$text-size = 1.5rem
$heading-size = 4rem

// Set default REM to 16px
html
    font-size 62.5%

// Global font styles
body
    font-family "Montserrat", sans-serif
    font-size $text-size
    // line-height 1.5

h1
    font-size 6rem

// to fill out
 
// h1 = $sans_serif
// h2 = $sans_serif
// h3 = $sans_serif
// p = $serif
// button = $sans_serif
// a = $serif

// below code should be moved to a different file
a
    text-decoration none
    color $bright_blue
    display inline-block
    width 100%
    box-sizing border-box

// a:hover
//     color $highlight-color

h1
    text-align center

h2
    margin 0
    padding 0
    
// Header font styles
.header
    font-size $text-size

// Banner font styles
.wt_banner-message h1, .wt_form-banner-message h1
    font-size $heading-size
    line-height 1

// Alert font styles
.wt_alert
    font-size 1.25rem
    //font-size $text-size

@media all and (max-width: 979px)
    h1
        font-size 3rem